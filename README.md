# Low to High Level Assembler

This is the in-progress assembler for LTHL.

Right now, it can handle a bunch of basic tasks, and can output to a final binary file.

This is then loaded as a ROM for the CPU.
